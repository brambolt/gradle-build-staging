
pluginManagement {
  repositories {
    mavenLocal()
    gradlePluginPortal()
    maven { url 'https://dl.bintray.com/brambolt/public' }
  }
  plugins {
    id 'com.brambolt.gradle.build.plugin' version bramboltVersion
  }
}

rootProject.name = 'gradle-build-staging'
